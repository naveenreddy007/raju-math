import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Raja - Mathematics Educator | LKG to IIT JEE",
  description: "Expert mathematics tutor specializing in comprehensive education from LKG to IIT JEE preparation. Experience the beauty of mathematics with interactive learning.",
  keywords: "mathematics tutor, IIT JEE preparation, math teacher, algebra, calculus, geometry, mathematical education",
  authors: [{ name: "Raja" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
